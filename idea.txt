Recieve the expression
	Store on a class named expression
	Parse the expression
		Detect errors
		if any error is present, advance to token step
	Tokenize the expression
		Define if it's a operand or a operator or a delimiter
		Convert to postfix form
	Run operations
		Run postfix operations
